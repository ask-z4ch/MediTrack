import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Backgrounds
  static const Color background = Color(0xFF0D1117);
  static const Color cardSurface = Color(0xFF161B27);
  static const Color cardElevated = Color(0xFF1E2435);

  // Brand
  static const Color primary = Color(0xFF00897B);
  static const Color primaryLight = Color(0xFF4DB6AC);

  // Text
  static const Color textPrimary = Color(0xFFE8EAED);
  static const Color textSecondary = Color(0xFF9AA0A6);
  static const Color textMuted = Color(0xFF5F6368);

  // Health status
  static const Color normal = Color(0xFF34A853);
  static const Color borderline = Color(0xFFFBBC04);
  static const Color critical = Color(0xFFEA4335);

  // Companion states (background glow colours)
  static const Color thriving = Color(0xFF1B5E20);
  static const Color stable = Color(0xFF0D47A1);
  static const Color concerned = Color(0xFFE65100);
  static const Color struggling = Color(0xFF4A148C);
  static const Color criticalBg = Color(0xFFB71C1C);
}
